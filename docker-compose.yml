

services:
  mongodb:
    image: mongo:latest
    container_name: expense_tracker_db
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
